---
task: aimin-skill 代码审查修复任务
created: 2026-05-20
---

# 修复任务清单

## P0 — 严重

### 1. mac.js 和 windows.js 完全重复

**问题**：`src/cli/install/mac.js` 和 `src/cli/install/windows.js` 每一行代码都完全相同，唯一区别是注释文案。平台分发到一个相同的文件，拆分无意义。

**改动**：合并为一个文件 `src/cli/install/platform.js`，删除 mac.js 和 windows.js。更新 `src/cli/install/index.js` 的 import。

**参考文件**：
- `src/cli/install/mac.js`
- `src/cli/install/windows.js`
- `src/cli/install/index.js:37-38` — import 行
- `src/cli/install/index.js:144-147` — 平台分发函数

### 2. `.agent/` vs `.agents/` 目录命名不一致

**问题**：archive 命令用 `.agent/archive/`，但 session 命令用 `.agents/archive/sessions/`，SKILL.md 和 README.md 中混用 `.agents/`。AI 无法确定正确的目录。

**改动**：统一为 `.agent/archive/`（与项目现有的 `.agent/` 一致）。需要更新：
- `commands/session.md` — `.agents/` → `.agent/`
- `skills/SKILL.md` — `.agents/` → `.agent/`
- `skills/README.md` — `.agents/` → `.agent/`
- `src/cli/templates.js` 中的 session 模板 — `.agents/` → `.agent/`

### 3. SKILL.md 引用了已废弃的目录结构

**问题**：`skills/SKILL.md:29` 的 Workflow 规则 7 仍引用 `.agents/archive/features/` 和 `.agents/archive/pages/`，但 archive 命令指南已废弃这些子目录，改用稳定主题文件直接放在 `.agent/archive/` 下。

**改动**：更新 `skills/SKILL.md` 规则 7，与 `commands/archive.md` 保持一致。

### 4. README.md 项目级规则表示例路径过时

**问题**：`skills/README.md:73` 的示例使用 `.agents/archive/pages/order-detail.md`，但 archive 命令指南已改为 `.agent/archive/order-detail.md`。

**改动**：更新 `skills/README.md` 的项目级规则表示例路径。

## P1 — 中等

### 5. Codex 命令 skill body 字符串替换脆弱

**问题**：`src/cli/templates.js:199-202` 对 `用户调用命令参数：$ARGUMENTS` 做字符串替换，如果模板中该字符串格式有变化则静默失败。

**改动**：在 `buildPromptBody` 中增加 `codexUserSkillLabel` 可选参数，当传入时直接生成正确文本，避免事后替换。

### 6. doctor 的 buildInstallContext 做了不必要的工作

**问题**：`doctor.js` 调用 `buildInstallContext()` 只是为了获取路径信息，但 `buildInstallContext` 内部读取了所有 8 个命令文件和枚举了 skill 目录。

**改动**：将路径构建逻辑从 `buildInstallContext` 中拆分为独立的轻量函数，doctor 调用轻量版本。

### 7. doctor 重复读取 manifest 文件

**问题**：`doctor.js:43` 在循环中为每个 tool 重复读取同一个 `context.manifestPath` JSON 文件。

**改动**：在循环外读取一次 manifest，传入 `createToolReport`。

### 8. buildPromptBody 使用 if-else 链

**问题**：`src/cli/templates.js:217-224` 使用 8 个 if-else 分支分发命令类型，不易维护。

**改动**：改用 `Map` 或对象查找：
```js
const BUILDERS = {
  init: buildInitPromptBody,
  api: buildApiPromptBody,
  // ...
};
return BUILDERS[command.key](options);
```

## P2 — 小优化

### 9. runToolCommand 在 Windows 上始终使用 shell

**问题**：`install/index.js:474` 的 `shell: process.platform === 'win32'` 导致参数经 cmd.exe 解析，有特殊字符风险。

**改动**：对已知简单命令（claude/codex plugin marketplace）不需要 shell，删掉 shell 参数或仅在需要时启用。

### 10. 读 manifest 的 regex 转义不必要

**问题**：`mac.js` 和 `windows.js:236` 对固定常量 `MARKETPLACE_NAME`（`'aimin-skill'`）做正则转义，且转义写法有重复反斜杠，可读性差。

**改动**：对已知安全字符串直接拼接，或简化转义逻辑。

### 11. package.json files 字段缺少 AGENTS.md

**问题**：`package.json` 的 `files` 数组未包含 `AGENTS.md`，npm publish 时会遗漏。

**改动**：在 `files` 数组中追加 `"AGENTS.md"`。
