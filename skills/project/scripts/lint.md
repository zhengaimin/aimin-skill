# lint SOP

交付前按以下顺序做收尾检查。

## 默认检查

1. 对本次修改文件执行 lint 校验，优先按文件路径或最小范围运行，不默认全量 lint
2. 对照 `.agent/naming.md` 与 `.agent/comment.md` 检查命名、代码顺序、`handle*` 事件命名、常量命名和注释规范
3. 删除无语义缩写、临时命名和无效注释
4. 检查新增的接口、常量、公共方法是否已同步到对应索引文件
