# lint SOP

交付前按以下顺序做收尾检查。

## 默认检查

1. 对照 `.agent/naming.md` 检查命名、代码顺序、`handle*` 事件命名和常量命名
2. 删除无语义缩写、临时命名和无效注释
3. 检查新增的接口、常量、公共方法是否已同步到对应索引文件

## admin 额外检查

如果项目类型是 admin，还要额外执行：

1. 删除未使用变量
2. 删除未使用导入
3. 删除未使用参数或无意义占位变量
4. 检查表格、弹窗相关页面是否遵守 admin 规则
